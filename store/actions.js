import { TAB_INDEX } from './mutation-types'

console.log(TAB_INDEX)

export const addToCart = (product) => {
  if (product.inventory > 0) {
    // commit(types.ADD_TO_CART, {
    //   id: product.id
    // })
  }
}
